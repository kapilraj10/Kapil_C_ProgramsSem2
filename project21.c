#include <stdio.h>
    int main (){
        int a = 10;
        int b = 30;

// Check Relationships and print result 
printf("a == b : %d\n", a == b); // a equal to b
printf("a != b :%d\n", a != b); // a not equal to b
printf("a < b : %d\n", a < b); // a less then b
printf("a <= b : %d\n", a <= b); // a less than or equal to b
printf("a > b : %d\n", a > b); // a greater than b
printf("a >= b  : %d\n", a >= b); // a greater than or equal to b 
    
    return 0;
    }